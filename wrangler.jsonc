{
  // Cloudflare Worker name (shows in dashboard)
  "name": "lungpage",
  // Use today's date for Workers runtime compatibility
  "compatibility_date": "2025-09-09",
  // Static assets mode: serve files from repo root
  "assets": {
    "directory": ".",
    // assets-only Worker: no binding allowed
  }
}
